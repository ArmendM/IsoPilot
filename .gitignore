node_modules/
.next/
out/
build/
dist/

# Geheimnisse und echte Daten
.env
.env.*
!.env.example
# Zeigt, wie .env.test auszusehen hat. Die echte .env.test bleibt lokal.
!.env.test.example
secrets/
uploads/
backup/
*.dump
*.dump.age
*.sql
# Migrationen sind Quellcode, nicht Daten. Ohne sie hat
# prisma migrate deploy auf dem Server nichts anzuwenden.
!prisma/migrations/**/*.sql

# Werkzeuge
.DS_Store
*.log
*.tsbuildinfo
# Von Next erzeugt und je nach letztem Lauf verschieden.
# next typegen legt die Datei in der CI vor tsc wieder an.
next-env.d.ts
coverage/
.vscode/
!.vscode/extensions.json

# Zwischenspeicher von Vitest, entsteht beim Lauf mit Berichtsausgabe.
.vitest/

# Playwright
/test-results/
/playwright-report/
